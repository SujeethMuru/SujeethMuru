# Profile banner

`banner-v2.png` is the current GitHub profile header. It uses a wide abstract systems illustration with a high-contrast name and role lockup designed to remain legible at GitHub's profile width.

Palette: charcoal `#0B1118`, navy `#10233F`, cyan `#38BDF8`, amber `#F59E0B`.

The previous `banner.png` is retained as an archive and is no longer referenced by the profile README.
